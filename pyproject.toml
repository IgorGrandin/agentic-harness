[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-harness-runtime"
version = "0.3.0"
description = "Portable deterministic enforcement for native agent orchestration"
requires-python = ">=3.11"
dependencies = []

[tool.agentic-harness]
direct-runtime = "bin"
workflow-runtime = "native"
